services:
  nextjs-app:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run build && npm run start"
    ports:
      - "3001:3000"
    volumes:
      - ./foldersourcecodenextjsmu:/app
